name: design_system_generator_example
description: Example
version: 0.0.0
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.29.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.8.0
  design_system_generator:
    path: ../
